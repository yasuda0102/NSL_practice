// Driver for adder16

#include "declaration.nsl"

declare driver simulation {

}

module driver {
    adder32 adder;
    shifter s32;

    _display("carry = %x, result = %x", adder.carry, adder.add(0xffffffff, 0x00000001));

    _finish("%x", s32.shiftRightA(0x7fffffff, 5));
    //_finish("%x", s32.shiftRightL(0xffffffff, 4));
}