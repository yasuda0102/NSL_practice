// Driver for adder16

#include "declaration.nsl"

declare driver simulation {

}

module driver {
    adder32 adder;

    adder.a = 0xffffffff;
    adder.b = 0x00000001;
    _finish("carry = %x, result = %x", adder.carry, adder.c);
}