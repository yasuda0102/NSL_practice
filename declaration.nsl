declare adder32 {
    input a[32];
    input b[32];
    output c[32];
    output carry;
}

declare shifter {
    input inp[32];
    output result[32];
    input arithmetic;
    input num[6]; // 0～32bitまでのシフトに対応
}